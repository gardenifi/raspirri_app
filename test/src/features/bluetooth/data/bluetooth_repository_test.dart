import 'package:flutter_blue_plus/flutter_blue_plus.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
import 'package:new_gardenifi_app/src/features/bluetooth/data/bluetooth_repository.dart';

import '../../../mocks.dart';

void main() {
  test('Turn on bluetooth', () {
    // final bluetoothRepository = MockBluetoothRepo();
    // final blueRepo = BluetoothRepository();

    // when(() => bluetoothRepository.turnBluetoothOn()).thenAnswer((_) => Future.value());
    // bluetoothRepository.turnBluetoothOn();
    // verify(MockFlutterBluePlus.turnOn).called(1);
  });
}
